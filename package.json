{
  "name": "trmnl-jazz-lineup",
  "version": "1.1.0",
  "private": true,
  "description": "Open-source Jazz Lineup feed and TRMNL Recipe",
  "type": "module",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "validate:feed": "node scripts/validate-feed.mjs public/nyc.json",
    "test": "node --test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.7.0",
    "prettier": "^3.9.5"
  }
}
